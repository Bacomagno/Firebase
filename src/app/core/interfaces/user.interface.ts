// definición: declaración de atributos que debe llevar un objeto (contrato que se debe cumplir)
// estudiar diferencia con una clase

export interface IUser {
    name: string;
    position: number;
    identificate: number;
    symbol: string;
}